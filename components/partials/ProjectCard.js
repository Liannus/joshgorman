import react from 'react';

export default class ProjectCard extends react.component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <>
        <p>some information inside of the about page</p>
      </>
    );
  }
}
